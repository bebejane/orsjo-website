#import "./fragments/ProductFragment.graphql"

query GetProducts($locale: SiteLocale = en, $first: IntType = 100, $skip: IntType = 0) {
	products: allProducts(locale: $locale, first: $first, skip: $skip, orderBy: [title_ASC], fallbackLocales: [sv, en]) {
		...ProductFragment
	}
}