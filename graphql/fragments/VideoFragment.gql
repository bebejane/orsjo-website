fragment VideoFragment on FileField {
	id
	mimeType
	url
	title
	customData
	responsiveImage(imgixParams: { auto: format }) {
		src
		width
		height
		alt
		base64
		sizes
	}
	video {
		thumbnailUrl
		streamingUrl
		framerate
		duration
	}
}
