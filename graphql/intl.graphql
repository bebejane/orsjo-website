#import "./fragments/ProductFragment.graphql"

query GetIntl( $locale: SiteLocale = en, $first: IntType = 100, $skip: IntType = 0) {
	messages: allTranslations(locale: $locale, first: $first, skip: $skip, orderBy: [value_ASC]) {
		key
    value
	}
}

